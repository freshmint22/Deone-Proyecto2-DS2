# Backend environment example
# Copy this file to backend/.env and fill in real values (do NOT commit secrets)
MONGO_URI=
JWT_SECRET=
FRONTEND_ORIGIN=
SEED_SECRET=
PORT=4000
# Example MongoDB URI for local development
MONGO_URI=mongodb://localhost:27017/deone_db
# JWT secret used to sign tokens (replace with a secure string in production)
JWT_SECRET=your_jwt_secret_here
